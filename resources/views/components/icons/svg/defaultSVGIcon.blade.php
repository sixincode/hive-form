<svg xmlns="http://www.w3.org/2000/svg" stroke-width="{{$size}}" class="h-{{$height}} w-{{$width}} {{$class ?? ''}}" fill="{{$fill ?? 'currentColor'}}" viewBox="{{$viewBox}}" stroke="{{$stroke ?? currentColor}}" aria-hidden="true">
   {!!$schema!!}
</svg>
